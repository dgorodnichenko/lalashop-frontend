import { MainProductCategory } from './main-product-category';

describe('MainProductCategory', () => {
  it('should create an instance', () => {
    expect(new MainProductCategory()).toBeTruthy();
  });
});
