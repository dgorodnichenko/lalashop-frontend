import { SubproductCategory } from './subproduct-category';

describe('SubproductCategory', () => {
  it('should create an instance', () => {
    expect(new SubproductCategory()).toBeTruthy();
  });
});
